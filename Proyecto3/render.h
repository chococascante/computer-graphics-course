/*
 * render.h — Loop principal del raytracer: integra cámara, sombreado,
 * salida a archivo y vista SDL en tiempo real.
 */
#ifndef RENDER_H
#define RENDER_H

#include "tipos.h"

int renderizar(const Escena *esc);

#endif
